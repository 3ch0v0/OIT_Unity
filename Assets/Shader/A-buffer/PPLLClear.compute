#pragma kernel AClear

RWByteAddressBuffer startOffetBuffer;
int _ScreenWidth;

[numthreads(8, 8, 1)]
void AClear (uint3 id: SV_DispatchThreadID)
{
    uint index = id.y * _ScreenWidth + id.x;
    startOffetBuffer.Store(index * 4, 0);
}